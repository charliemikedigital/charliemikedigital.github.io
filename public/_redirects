# Cloudflare Pages routing rules
#
# Rules are evaluated top-to-bottom. The SPA fallback at the bottom
# routes anything that's not a real file/directory to the React app
# so client-side anchors keep working.
#
# Real files in /public (privacy/, terms/, sites/, brand/) resolve
# directly to their on-disk content before this fallback fires.

/*    /index.html    200
